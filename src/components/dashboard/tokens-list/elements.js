import styled from 'styled-components';

const MessageContainer = styled.div`
  display: flex;
  margin-top: 20px;
  text-align: center;
  flex-direction: column;
  align-items: center;
`;

const TableContainer = styled.div`
  display: block;
  overflow-x: auto;
`;

export { MessageContainer, TableContainer };
